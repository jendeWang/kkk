# 智慧农业物联网平台 - 产品使用手册

## 目录

```
第1章 快速入门
第2章 仪表盘
第3章 产品与设备管理
第4章 场景管理
第5章 数据报表
第6章 告警管理
第7章 命令下发
第8章 系统管理
第9章 高级功能（可选）
```

---

## 第1章 快速入门

本章将帮助您快速了解如何使用智慧农业物联网平台。

### 1.1 登录与账号

#### 登录系统

**操作步骤：**

1. 打开浏览器，输入平台地址，进入登录页面
   ![登录页面](images/00-login.png)

2. 在"用户名"输入框中输入您的账号
3. 在"密码"输入框中输入您的密码
4. 点击"登录"按钮进入系统

**温馨提示：**
- 首次使用请使用管理员分配的账号
- 密码区分大小写，请注意输入正确
- 连续输入错误5次后，账号会被锁定15分钟

---

### 1.2 认识主界面

登录成功后，系统进入主界面，主要包含以下区域：

![仪表盘](images/01-dashboard.png)

| 区域 | 说明 |
|------|------|
| 顶部导航栏 | 显示当前页面名称、搜索框、消息通知、用户菜单 |
| 左侧菜单栏 | 功能导航菜单，包含仪表盘、产品、设备、场景等 |
| 主内容区 | 显示当前功能模块的具体内容 |
| 底部状态栏 | 显示当前登录用户、在线设备数量 |

**顶部导航栏功能说明：**
- **Logo图标**：点击可快速返回仪表盘首页
- **搜索框**：支持快速搜索设备、产品等信息
- **消息图标**：查看系统通知和告警信息
- **用户头像**：点击展开用户菜单

---

### 1.3 退出登录

**操作步骤：**

1. 点击页面右上角的用户头像
   ![用户菜单](images/15-user-menu.png)

2. 在下拉菜单中点击"退出登录"按钮

3. 系统提示"确定要退出吗？"，点击"确定"

4. 页面跳转到登录页面

**温馨提示：**
- 退出登录后，所有未保存的数据将会丢失
- 建议在公用电脑上使用时，务必退出登录
- 系统支持30分钟无操作自动退出

---

## 第2章 仪表盘

仪表盘是系统的首页，展示农场的整体运行状态。

### 2.1 查看农场概况

仪表盘顶部以卡片形式展示农场的关键数据：

| 数据项 | 说明 |
|--------|------|
| 设备总数 | 当前已接入的所有设备数量 |
| 在线设备 | 当前正常运行的设备数量 |
| 今日告警 | 今日产生的告警事件数量 |
| 命令下发 | 今日下发的控制命令数量 |

![仪表盘概览](images/01-dashboard.png)

**如何刷新数据：**
- 页面数据会自动每30秒刷新一次
- 点击卡片右上角的刷新按钮可手动刷新

---

### 2.2 环境数据总览

仪表盘中部的图表区域展示环境监测数据：

**查看步骤：**

1. 在仪表盘中部找到"环境数据"图表区域

2. 图表默认显示近24小时的数据趋势

3. 将鼠标移到图表数据点上，可查看具体数值

4. 点击图表上方的"温度"、"湿度"、"光照"等标签，可切换显示不同指标

**图表操作说明：**
- **缩放**：使用鼠标滚轮可放大或缩小时间范围
- **平移**：按住鼠标左键拖动可移动查看时间范围
- **重置**：点击右上角"重置"按钮恢复默认视图

---

### 2.3 设备状态一目了然

仪表盘底部的设备状态列表展示所有设备：

**查看设备状态：**

1. 在仪表盘底部找到"设备状态"区域

2. 设备以列表形式展示，每行显示一个设备

3. 列表包含：设备名称、所属产品、在线状态、最后上报时间

4. **状态指示：**
   - 🟢 绿色图标：设备在线
   - 🔴 红色图标：设备离线
   - 🟡 黄色图标：设备告警

**快速定位问题设备：**
- 点击"只看离线"按钮，筛选出离线设备
- 点击"只看告警"按钮，筛选出告警设备

---

### 2.4 快捷操作入口

仪表盘右侧提供常用功能的快捷入口：

| 功能 | 说明 |
|------|------|
| 告警事件 | 快速查看最新告警，点击进入告警详情 |
| 场景联动 | 快速进入场景管理，创建自动化规则 |
| 数据大屏 | 进入数据可视化大屏，全屏展示数据 |
| 沙盘拓扑 | 查看设备拓扑关系图 |

**操作步骤：**

1. 点击任意快捷入口卡片

2. 系统直接跳转到对应功能页面

3. 无需通过左侧菜单层层查找

---

## 第3章 产品与设备管理

### 3.1 产品管理

产品是对一类设备的抽象定义，包含设备的公共属性和功能。

#### 查看产品列表

**操作步骤：**

1. 在左侧菜单中点击"产品管理"
   ![产品管理](images/04-products.png)

2. 进入产品管理页面，查看所有已创建的产品

3. 产品列表显示：产品名称、产品类型、协议类型、创建时间、设备数量

#### 创建新产品

**操作步骤：**

1. 在产品管理页面，点击右上角"新增产品"按钮

2. 在弹出的表单中填写以下信息：
   - **产品名称**：输入产品名称（如：温室大棚）
   - **产品类型**：选择设备类型
   - **协议类型**：选择通信协议（MQTT/CoAP/HTTP）
   - **数据格式**：选择数据格式（JSON/二进制）

3. 点击"确认"按钮完成创建

4. 创建成功后，自动进入产品详情页面

#### 管理产品

**查看产品详情：**
1. 在产品列表中点击产品名称，进入产品详情

**编辑产品：**
1. 在产品详情页面，点击右上角"编辑"按钮
2. 修改需要更新的信息
3. 点击"保存"按钮

**删除产品：**
1. 在产品列表中，点击产品右侧的"删除"按钮
2. 系统提示"删除后无法恢复，确定删除吗？"
3. 点击"确定"完成删除
4. **注意**：产品下存在设备时无法删除

---

### 3.2 设备管理

设备是实际存在的物联网终端设备。

#### 查看设备列表

**操作步骤：**

1. 在左侧菜单中点击"设备管理"
   ![设备管理](images/06-devices.png)

2. 进入设备管理页面，查看所有设备

3. 设备列表显示：设备名称、设备编码、产品类型、在线状态、创建时间

#### 添加新设备

**操作步骤：**

1. 在设备管理页面，点击右上角"新增设备"按钮

2. 在弹出的表单中填写以下信息：
   - **设备名称**：输入设备名称
   - **所属产品**：从下拉列表选择产品
   - **设备编码**：输入唯一编码（如：DEVICE_001）
   - **安装位置**：输入设备安装位置
   - **经纬度**：输入设备GPS坐标（可选）

3. 点击"确认"按钮完成添加

4. 添加成功后，系统显示设备接入凭证（DeviceToken）

#### 管理设备

**查看设备详情：**
1. 在设备列表中点击设备名称，进入设备详情页面

**编辑设备信息：**
1. 在设备详情页面，点击右上角"编辑"按钮
2. 修改设备信息
3. 点击"保存"按钮

**删除设备：**
1. 在设备详情页面，点击右上角"删除"按钮
2. 确认删除后，设备将从列表中移除

---

### 3.3 分组管理

设备分组功能可以按区域或类型对设备进行归类管理。

#### 查看分组

**操作步骤：**

1. 在左侧菜单中点击"设备分组"
   ![设备分组](images/10-groups.png)

2. 进入分组管理页面

3. 分组以树形结构展示，最顶层为默认分组

#### 创建分组

**操作步骤：**

1. 在分组管理页面，点击右上角"新建分组"按钮

2. 在弹出的表单中填写：
   - **分组名称**：输入分组名称（如：1号大棚）
   - **上级分组**：选择上级分组（默认为根分组）
   - **分组描述**：输入分组说明

3. 点击"确认"按钮完成创建

#### 分配设备到分组

**操作步骤：**

1. 在分组管理页面，点击分组右侧的"分配设备"按钮

2. 在设备列表中勾选要分配到该分组的设备

3. 点击"确认"按钮完成分配

4. 设备可以属于多个分组

---

## 第4章 场景管理

场景联动功能可以实现设备的自动化控制。

### 4.1 创建场景联动

**操作步骤：**

1. 在左侧菜单中点击"场景联动"
   ![场景联动](images/03-scenes.png)

2. 进入场景管理页面，点击右上角"创建场景"按钮

3. 在创建页面填写以下信息：

   **基本信息：**
   - **场景名称**：输入场景名称（如：高温自动通风）
   - **触发条件**：设置触发条件的设备、指标和阈值

   **执行动作：**
   - **执行设备**：选择要控制的设备
   - **控制指令**：选择要下发的命令（如：开启、关闭）
   - **延时设置**：设置命令执行延迟时间

4. 点击"保存"按钮完成创建

**场景联动示例：**

| 场景名称 | 触发条件 | 执行动作 |
|----------|----------|----------|
| 高温通风 | 温度 > 35°C | 开启风扇 |
| 干旱灌溉 | 土壤湿度 < 30% | 开启水泵 |
| 光照不足 | 光照强度 < 200 lux | 开启补光灯 |
| 夜间告警 | 时间 22:00-06:00 + 检测到移动 | 发送告警通知 |

### 4.2 管理场景规则

**启用/停用场景：**

1. 在场景列表中，点击场景右侧的开关按钮
2. 开启：开关显示蓝色，场景生效
3. 关闭：开关显示灰色，场景暂停

**编辑场景：**
1. 点击场景右侧的"编辑"按钮
2. 修改场景配置信息
3. 点击"保存"按钮

**删除场景：**
1. 点击场景右侧的"删除"按钮
2. 确认删除后，场景将被移除

**查看执行记录：**
1. 点击场景右侧的"日志"按钮
2. 查看该场景的触发时间和执行结果

---

## 第5章 数据报表

### 5.1 遥测数据查询

遥测数据是设备上报的传感器数据。

**操作步骤：**

1. 在左侧菜单中点击"遥测数据"
   ![遥测数据](images/07-telemetry.png)

2. 进入遥测数据查询页面

3. 设置查询条件：
   - **设备选择**：选择要查询的设备（支持多选）
   - **时间范围**：选择查询的时间段
   - **数据指标**：选择要查询的指标类型

4. 点击"查询"按钮

5. 查询结果以表格形式展示，包含：时间、设备、指标名称、指标值

**数据可视化：**
- 点击"图表"视图，可查看数据趋势图
- 支持折线图、柱状图切换
- 可导出数据为CSV格式

### 5.2 导出历史数据

**操作步骤：**

1. 在遥测数据页面，设置好查询条件

2. 点击右上角"导出"按钮

3. 选择导出格式（CSV/Excel）

4. 选择导出时间范围

5. 点击"导出"开始下载

6. 导出完成后，浏览器自动下载文件

**温馨提示：**
- 单次导出最大支持10万条数据
- 大数据量导出可能需要等待较长时间

---

## 第6章 告警管理

### 6.1 查看告警事件

**操作步骤：**

1. 在左侧菜单中点击"告警事件"
   ![告警事件](images/02-alerts.png)

2. 进入告警事件页面，查看所有告警记录

**统计卡片：**

页面顶部以卡片形式展示告警统计数据：

| 卡片 | 说明 |
|------|------|
| 总告警 | 最近7天内的告警总数 |
| 严重 | 严重级别的告警数量 |
| 警告 | 警告级别的告警数量 |
| 待处理 | 尚未处理的告警数量 |

**搜索与筛选：**

1. 在搜索框中输入设备名称或告警信息，实时筛选
2. 使用"状态筛选"下拉框选择：全部/待处理/已确认/已解决
3. 使用"级别筛选"下拉框选择：全部/严重/错误/警告/信息
4. 点击"刷新"按钮重新加载数据

**告警列表显示：**

告警列表显示以下信息：
- **告警信息**：告警的详细描述
- **设备**：触发告警的设备名称
- **监测指标**：触发告警的传感器指标
- **告警级别**：告警的严重程度
- **当前值**：触发告警时的指标数值
- **阈值**：告警触发的阈值
- **状态**：告警处理状态
- **创建时间**：告警产生的时间

**告警级别说明：**

| 级别 | 图标颜色 | 说明 |
|------|----------|------|
| 严重 | 🔴 红色 | 需要立即处理 |
| 错误 | 🟠 橙色 | 需要尽快处理 |
| 警告 | 🟡 黄色 | 可以稍后处理 |
| 信息 | 🔵 蓝色 | 信息提醒 |

**处理告警：**

1. **确认告警**：点击"确认"按钮，表示已看到并开始处理该告警
   - 确认后告警状态变为"已确认"

2. **解决告警**：点击"解决"按钮，表示问题已处理完毕
   - 解决后告警状态变为"已解决"

**操作说明：**
- 只有"待处理"状态的告警可以点击"确认"按钮
- 任何非"已解决"状态的告警都可以点击"解决"按钮
- 操作成功后，页面会自动刷新并显示最新状态

### 6.2 配置告警规则

**操作步骤：**

1. 在左侧菜单中点击"告警规则"
   ![告警规则](images/09-alert-rules.png)

2. 进入告警规则配置页面

3. 点击右上角"新增规则"按钮

4. 在表单中填写规则配置：

   **规则信息：**
   - **规则名称**：输入规则名称
   - **所属产品**：选择关联的产品
   - **指标类型**：选择要监测的指标

   **触发条件：**
   - **条件类型**：选择条件（大于、小于、等于、区间）
   - **阈值**：输入触发阈值
   - **持续时间**：设置告警触发需要满足条件的持续时间

   **告警设置：**
   - **告警级别**：选择告警级别
   - **告警方式**：勾选通知方式（短信/邮件/站内消息）
   - **通知对象**：选择接收通知的人员

5. 点击"保存"按钮完成配置

**告警规则示例：**

| 规则名称 | 监测指标 | 触发条件 | 告警级别 |
|----------|----------|----------|----------|
| 温度过高 | 温度 | > 35°C 持续5分钟 | 紧急 |
| 湿度过低 | 土壤湿度 | < 30% 持续10分钟 | 重要 |
| 设备离线 | 在线状态 | 离线超过30分钟 | 重要 |
| 电量不足 | 电量 | < 20% | 一般 |

---

## 第7章 命令下发

### 7.1 下发控制命令

通过平台可以远程控制设备执行特定操作。

**操作步骤：**

1. 在左侧菜单中点击"命令下发"
   ![命令下发](images/08-commands.png)

2. 进入命令下发页面

3. 点击右上角"下发命令"按钮

4. 在弹出的表单中填写：
   - **目标设备**：选择要控制的设备
   - **命令类型**：选择命令类型（如：开关、调节参数）
   - **命令参数**：输入命令参数值

5. 点击"确认"按钮下发命令

6. 命令下发后，系统显示下发结果

**常用命令示例：**

| 命令名称 | 适用设备 | 命令参数 | 说明 |
|----------|----------|----------|------|
| 开关控制 | 风扇、水泵、灯 | on/off | 开启或关闭设备 |
| 调节转速 | 变频风机 | 转速值(0-100) | 调节设备转速 |
| 设置温度 | 温控器 | 温度值 | 设置目标温度 |
| 重启设备 | 所有设备 | 无 | 重启设备 |

### 7.2 查看命令记录

**操作步骤：**

1. 在命令下发页面，点击"命令记录"标签

2. 查看所有下发过的命令记录

3. 记录列表显示：下发时间、设备名称、命令类型、参数、状态

**命令状态说明：**

| 状态 | 说明 |
|------|------|
| 已发送 | 命令已下发，等待设备响应 |
| 发送成功 | 设备已接收并执行命令 |
| 发送失败 | 命令下发失败 |
| 超时 | 设备未在规定时间内响应 |

**重新下发：**
- 对于发送失败或超时的命令，可点击"重试"按钮重新下发

---

## 第8章 系统管理

### 8.1 用户管理

用户管理功能允许管理员对系统用户进行管理，包括创建、编辑、禁用和删除用户。

**进入用户管理页面：**

1. 在左侧菜单中点击"用户管理"（仅管理员可见）

2. 进入用户管理页面，查看所有用户列表

**用户列表说明：**

| 字段 | 说明 |
|------|------|
| ID | 用户唯一标识 |
| 用户名 | 用户登录账号 |
| 姓名 | 用户真实姓名 |
| 邮箱 | 用户邮箱地址 |
| 状态 | 正常/禁用 |
| 角色 | 管理员/操作员/查看员 |
| 权限 | 超级用户/普通 |
| 创建时间 | 用户账号创建日期 |

**系统角色说明：**

平台采用 RBAC 角色权限管理，内置三种角色：

| 角色 | 权限范围 | 适用人员 |
|------|---------|---------|
| 👑 **管理员** (admin) | 全部权限：用户管理、设备管理、数据管理、告警规则、系统配置等 | 系统管理员、农场主 |
| 🔧 **操作员** (operator) | 设备操作、数据查看、告警配置、场景联动配置，不可管理用户 | 农场技术人员、值班人员 |
| 👁️ **查看员** (viewer) | 仅能查看设备状态和数据，不可进行任何操作 | 参观人员、客户、普通员工 |

**搜索用户：**

1. 在搜索框中输入用户名或姓名
2. 系统实时筛选匹配的用户

**创建用户：**

1. 点击右上角"新增用户"按钮
2. 在表单中填写：
   - **用户名**：登录账号（至少3位）
   - **密码**：登录密码（至少6位）
   - **姓名**：用户真实姓名
   - **邮箱**：用户邮箱地址
   - **角色**：选择用户角色（管理员/操作员/查看员）
   - **超级用户**：是否为超级用户（拥有全部权限）
3. 点击"确定"按钮完成创建

**编辑用户：**

1. 在用户列表中，点击用户右侧的"编辑"按钮
2. 修改用户信息（用户名不可修改）
3. 可调整用户角色和权限
4. 点击"确定"按钮保存

**启用/禁用用户：**

1. 在用户列表中，点击用户右侧的"禁用"或"启用"按钮
2. 确认操作后，用户状态更新
3. 禁用的用户将无法登录系统

**删除用户：**

1. 在用户列表中，点击用户右侧的"删除"按钮
2. 确认删除操作
3. **注意**：无法删除自己的账号

### 8.2 个人中心

个人中心允许用户查看和修改自己的个人信息及密码。

**进入个人中心：**

1. 点击页面右上角的用户头像
2. 在下拉菜单中点击"个人中心"

#### 查看个人信息

个人信息页面显示：

| 字段 | 说明 |
|------|------|
| 用户名 | 登录账号（不可修改） |
| 姓名 | 用户真实姓名 |
| 邮箱 | 用户邮箱地址 |
| 状态 | 当前账号状态 |
| 权限 | 用户权限级别 |
| 创建时间 | 账号创建日期 |

**修改个人信息：**

1. 在"个人信息"标签页中
2. 修改姓名和邮箱
3. 点击"保存修改"按钮

#### 修改密码

**操作步骤：**

1. 切换到"修改密码"标签页

2. 填写以下信息：
   - **当前密码**：输入当前登录密码
   - **新密码**：输入新密码（至少6位）
   - **确认新密码**：再次输入新密码

3. 点击"修改密码"按钮

4. 修改成功后，系统自动退出并跳转至登录页面

**温馨提示：**
- 新密码不能与当前密码相同
- 密码长度至少6位
- 修改密码后请重新登录

### 8.3 帮助与关于

#### 查看帮助文档

**操作步骤：**

1. 在设置页面，点击"帮助文档"标签

2. 查看平台的使用帮助

3. 点击目录可快速跳转到对应章节

#### 查看系统信息

**操作步骤：**

1. 在设置页面，点击"关于"标签

2. 查看系统版本、版权信息

3. 页面显示：
   - **系统版本**：当前平台版本号
   **版权声明**：版权所有方信息
   - **技术支持**：联系方式

---

## 第9章 高级功能（可选）

本章介绍平台的高级功能，适合技术人员和运维人员使用。

### 9.1 数字孪生3D大屏

数字孪生3D大屏以三维可视化方式展示大棚的实时状态，让您可以直观地看到大棚内的环境数据和设备运行情况。

**进入3D大屏：**

1. 在左侧菜单中点击"数字孪生"
   ![数字孪生](images/20-greenhouse-3d.png)

2. 进入3D数字孪生页面

**功能说明：**

| 区域 | 内容 |
|------|------|
| 顶部标题栏 | 显示平台名称和当前时间 |
| 左侧环境监测面板 | 温度、湿度、光照、土壤湿度实时数据 |
| 右侧设备控制面板 | 通风风扇、补光灯、卷帘、灌溉泵的开关控制 |
| 3D场景中央 | 大棚三维模型，包含框架、覆盖物、门、植物、传感器 |
| 底部状态栏 | 系统运行状态、设备在线数量、数据更新状态 |

**交互操作：**

| 操作 | 说明 |
|------|------|
| 鼠标左键拖拽 | 旋转视角，从不同角度查看大棚 |
| 鼠标滚轮 | 缩放视角，放大查看细节或缩小看全局 |
| 鼠标右键拖拽 | 平移视角 |
| 重置视角按钮 | 恢复到默认视角 |
| 自动旋转按钮 | 开启后视角自动环绕大棚旋转 |

**3D模型元素说明：**

| 元素 | 说明 |
|------|------|
| 🏠 大棚框架 | 金色金属框架，半透明蓝色覆盖物 |
| 🚪 大棚门 | 棕色木门，位于正前方 |
| 🌱 植物 | 绿色作物，分布在大棚内 |
| 📡 传感器标签 | 悬浮显示温湿度、光照、土壤数据 |
| 💨 通风风扇 | 开启时叶片旋转 |
| 💡 补光灯 | 开启时发光并照亮周围 |
| 🪟 卷帘 | 开启时下降，遮光保温 |

**设备控制：**

1. 在右侧"设备状态"面板中
2. 点击对应设备的开关按钮
3. 3D模型中的设备同步变化：
   - 风扇开启 → 叶片旋转
   - 补光灯开启 → 灯泡发光
   - 卷帘开启 → 卷帘下降
4. 操作结果实时反馈

**温馨提示：**
- 3D大屏对电脑性能有一定要求，配置较低时可能会有卡顿
- 按F11可进入全屏模式，获得更好的视觉体验
- 数据每秒自动更新，与实际设备状态同步

**设备交互操作：**

3D场景支持点击设备查看详情和控制，操作方式更加直观：

| 操作 | 说明 |
|------|------|
| 点击设备 | 弹出设备详情弹窗，显示设备状态、位置、详情信息 |
| 波纹效果 | 点击设备时产生蓝色波纹扩散动画，提供操作反馈 |
| 设备高亮 | 选中的设备会显示蓝色高亮框，方便识别 |
| 一键控制 | 在设备详情弹窗中可直接开启/关闭设备 |

**告警提醒：**

当系统检测到告警时，3D场景会显示红色闪烁效果，提醒用户注意：
- 告警触发时，全屏显示红色闪烁叠加层
- 闪烁效果持续2次，每次0.5秒
- 确保用户不会错过重要告警

---

### 9.2 数据大屏

数据大屏提供全屏展示的农场数据可视化界面，适合在监控中心或大屏幕上展示。

**进入数据大屏：**

1. 在左侧菜单中点击"数据大屏"
   ![数据大屏](images/05-big-screen.png)

2. 进入数据大屏页面

**功能说明：**

| 区域 | 内容 |
|------|------|
| 顶部标题栏 | 显示农场名称和当前时间 |
| 左侧数据面板 | 展示设备统计、告警统计等汇总数据 |
| 中间主图区 | 以图表形式展示环境监测数据趋势 |
| 右侧设备列表 | 实时显示设备在线状态 |
| 底部实时数据 | 滚动展示最新的传感器数据 |

**操作说明：**

- **全屏模式**：点击右上角"全屏"按钮，进入全屏展示模式
- **退出全屏**：按 ESC 键或点击右上角"退出"按钮
- **自动轮播**：大屏支持自动轮播多个数据视图
- **数据刷新**：数据每30秒自动刷新一次

**使用场景：**
- 监控中心大屏展示
- 领导视察数据汇报
- 农场展览展示

---

### 9.2 设备拓扑

设备拓扑以图形化方式展示设备之间的关系和连接状态。

**查看设备拓扑：**

1. 在左侧菜单中点击"设备拓扑"
   ![设备拓扑](images/12-topology.png)

2. 进入拓扑图页面

**拓扑图元素说明：**

| 元素 | 说明 |
|------|------|
| 节点 | 代表单个设备或设备组 |
| 连线 | 表示设备之间的通信连接 |
| 图标颜色 | 绿色=在线，红色=离线，黄色=告警 |

**操作说明：**

- **缩放**：使用鼠标滚轮缩放拓扑图
- **拖拽**：按住鼠标左键拖动可移动视图
- **查看详情**：点击设备节点，显示设备详情弹窗
- **筛选**：使用顶部筛选器按产品或状态筛选设备
- **布局切换**：支持树状图、星状图、环形图等布局

---

### 9.3 API密钥管理

API密钥用于第三方系统调用平台接口。

**查看API密钥：**

1. 在左侧菜单中点击"API密钥"
   ![API密钥管理](images/11-api-keys.png)

2. 进入API密钥管理页面

**功能说明：**

| 字段 | 说明 |
|------|------|
| 密钥名称 | 密钥的描述名称 |
| AppKey | 应用的唯一标识符 |
| AppSecret | 应用的密钥密码 |
| 创建时间 | 密钥创建日期 |
| 状态 | 启用/禁用状态 |

**创建新的API密钥：**

1. 点击右上角"创建密钥"按钮

2. 在弹出的表单中填写：
   - **密钥名称**：输入密钥描述（如：数据对接系统）
   - **权限范围**：选择接口权限（只读/读写）

3. 点击"确认"按钮

4. **重要**：在弹窗中复制并保存 AppKey 和 AppSecret，关闭后将无法再次查看

**使用API密钥：**

调用接口时需要在请求header中携带：
```
X-App-Key: your_app_key
X-App-Secret: your_app_secret
```

**安全建议：**
- 定期更换API密钥
- 不要在客户端代码中硬编码密钥
- 为不同系统创建不同的密钥，便于管理

---

### 9.4 API测试台

API测试台提供在线接口调试功能，无需编写代码即可测试平台接口。

**进入API测试台：**

1. 在左侧菜单中点击"API测试台"
   ![API测试台](images/13-api-playground.png)

2. 进入API测试台页面

**功能说明：**

| 区域 | 说明 |
|------|------|
| 接口列表 | 左侧展示所有可用API接口 |
| 请求参数区 | 中间填写接口请求参数 |
| 响应结果区 | 右侧显示接口返回结果 |

**测试API接口：**

1. 从左侧接口列表中选择要测试的接口

2. 在请求参数区填写：
   - **Headers**：设置请求头（如AppKey、AppSecret）
   - **参数**：填写接口需要的参数

3. 点击"发送"按钮

4. 在右侧查看：
   - **状态码**：请求结果状态
   - **响应时间**：接口响应耗时
   - **返回数据**：接口实际返回内容

**常用接口示例：**

| 接口名称 | 功能 | 适用场景 |
|----------|------|----------|
| 获取设备列表 | 查询所有设备 | 设备数据同步 |
| 获取遥测数据 | 查询传感器数据 | 数据分析 |
| 下发设备命令 | 控制设备 | 远程控制 |

**提示：**
- 测试台仅用于调试，正式环境请使用SDK或直接调用接口
- 测试环境数据与生产环境隔离

---

### 9.5 操作日志

操作日志记录所有用户在平台上的操作行为，便于审计和问题排查。

**查看操作日志：**

1. 在左侧菜单中点击"操作日志"
   ![操作日志](images/14-operation-logs.png)

2. 进入操作日志页面

**日志列表说明：**

| 字段 | 说明 |
|------|------|
| 操作时间 | 操作发生的具体时间 |
| 操作人 | 执行操作的用户账号 |
| 操作类型 | 操作类别（登录、创建、修改、删除等） |
| 操作对象 | 操作涉及的实体（如设备名称、产品名称） |
| 操作详情 | 具体的操作内容 |
| IP地址 | 操作时的用户IP地址 |

**筛选日志：**

1. 使用顶部筛选器可以按以下条件筛选：
   - **时间范围**：选择查询的时间段
   - **操作类型**：选择特定操作类型
   - **操作人**：输入用户名筛选

2. 点击"查询"按钮查看结果

**导出日志：**

1. 设置好筛选条件后

2. 点击右上角"导出"按钮

3. 选择导出格式（CSV/Excel）

4. 下载包含筛选结果的日志文件

**日志保留说明：**
- 操作日志默认保留90天
- 超过保留期限的日志会自动清理
- 如需更长期保存，可联系管理员配置备份策略

---

## 更新日志

### v1.3.0 (2026-07-08)

#### 新增功能

**1. 新增3种环境传感器监测**
- 土壤pH值监测：实时显示土壤酸碱度（正常范围5.5-7.5 pH）
- 风速监测：实时显示棚区风速（正常范围0-10 m/s）
- 雨量监测：实时显示降雨量（正常范围0-10 mm）
- 以上数据在仪表盘、数字孪生大屏、数据大屏、传感器监控、遥测数据中均同步展示

**2. 新增3种设备控制能力**
- 遮阳帘控制：可在数字孪生大屏中控制遮阳帘开合
- 电磁阀控制：可在数字孪生大屏中控制滴灌电磁阀开关
- 加热膜控制：可在数字孪生大屏中控制加热膜开关

#### 功能优化

**1. 3D数字孪生大屏WebGL降级处理**
- 当浏览器不支持WebGL时，自动显示友好的降级提示页面
- 降级模式下，所有环境数据和设备控制功能仍可正常使用
- 数据加载与3D渲染完全解耦，确保极端情况下数据可用

**2. 趋势图功能扩展**
- 新增土壤温度、土壤pH、风速、雨量4种趋势图展示
- 支持8种环境参数的历史趋势查看（最近6小时）

**3. 设备命令接口统一**
- 统一设备命令下发接口规范
- 支持多种服务标识符兼容（set_fan/fan_switch等）
- 批量命令接口同步扩展支持

**4. 数据大屏功能扩展**
- 数据大屏新增土壤pH、风速、雨量3项传感器展示
- 共9项环境数据可视化展示，配备渐变进度条
- 用于领导视察、展厅展示等场景

**5. 告警规则支持新传感器**
- 告警规则创建时可选择土壤pH、风速、雨量作为监测指标
- 支持大于、小于、等于等多种阈值条件
- 告警消息自动识别新传感器名称和单位

**6. 场景联动支持新传感器触发**
- 场景联动创建时可选择土壤pH、风速、雨量作为触发条件
- 例如：风速超过10m/s自动关闭遮阳帘
- 例如：雨量超过5mm自动关闭通风扇

**7. 产品物模型自动同步**
- 系统启动时自动检测并补全缺失的产品属性定义
- 自动检测并补全缺失的服务定义
- 确保升级后数据和功能完整性

**8. 属性映射修正**
- 修复"降雨量"标识符拼写问题（rain_fall → rainfall）
- 补充土壤pH值单位（pH）
- 统一全系统属性命名规范

**9. RBAC角色权限系统**
- 新增三种系统角色：管理员、操作员、查看员
- 管理员：全部权限，可管理用户和系统配置
- 操作员：可操作设备、配置告警和规则，不可管理用户
- 查看员：仅可查看数据，不可进行任何操作
- 用户管理页面支持角色分配和调整
- 顶部导航栏显示当前用户角色标签
- 菜单根据角色权限自动显示/隐藏
- 数据库自动迁移，兼容旧系统升级

**10. 操作日志与审计**
- 完整的操作日志记录功能
- 记录所有用户操作行为（新增、修改、删除等）
- 支持按操作类型、操作用户、时间范围筛选
- 可追溯系统操作历史，满足审计需求

**11. 3D数字孪生场景动画效果**
- 风扇旋转动画：根据风速动态调整旋转速度
- 遮阳帘开合动画：平滑升降效果
- 下雨水滴效果：雨量>0时在大棚外部显示雨滴，随风速偏移
- 灌溉水滴效果：水泵开启时从出水口喷出水滴
- 补光灯开关效果：开启时发出暖黄色光芒
- 电磁阀状态指示灯：开启时发出橙色光芒
- 加热膜状态指示灯：开启时发出红色光芒

**12. 3D场景交互反馈增强（P0）**
- 点击设备放大特写：点击任意设备时，视角自动拉近放大该设备
- 波纹粒子反馈效果：点击设备时产生蓝色波纹扩散动画，提供即时操作反馈
- 告警闪烁提醒：系统检测到告警时，全屏显示红色闪烁叠加层，确保用户不会错过重要告警
- 设备详情弹窗：点击设备弹出详情弹窗，显示设备状态、位置、详情信息，支持一键控制

**13. 代码模块化改造（为拆分做准备）**
- 后端模块化：按业务领域划分为core、devices、ai、agents、knowledge模块，路由独立管理
- 前端模块化：按功能模块组织组件和页面，支持后续独立部署
- 微服务架构准备：为P3边缘AI服务、P4智能体+知识图谱服务预留独立模块入口，便于后续单独部署调试

**14. 可视化物模型编辑器**
- 图形化界面管理产品的属性、服务和事件
- 快捷添加常用传感器模板（温度、湿度、光照、土壤湿度、CO₂、风速、土壤pH、雨量、土壤温度）
- 快捷添加常用执行器服务（通风扇、补光灯、水泵、遮阳帘、电磁阀、加热膜）
- 支持属性的增删改查，可配置数据类型、访问类型、单位、取值范围等
- 支持服务的增删改查，可配置输入参数和输出参数
- 支持事件的增删改查，可配置事件类型和输出参数
- 一键保存所有修改，自动同步到数据库

**15. 模板市场**
- 提供4个预设产品模板，覆盖农业和畜牧两大领域
- 智慧大棚·基础版：4传感器+3执行器+3告警+2场景，适合小型种植户
- 智慧大棚·标准版：6传感器+4执行器+5告警+4场景，适合中型农场（推荐）
- 智慧大棚·高级版：12传感器+6执行器+8告警+6场景，适合大型种植基地/科研用途
- 智慧养殖·基础版：5传感器+3执行器+4告警+3场景，适合畜禽养殖户
- 支持按分类（农业/畜牧）和级别（入门/推荐/专业）筛选模板
- 模板详情预览：展示传感器数量、执行器数量、告警规则、自动化场景
- 一键从模板创建完整产品，自动生成物模型、告警规则和自动化场景

#### Bug修复

**1. 新增设备最新遥测数据接口**
- 新增接口 `GET /api/v1/devices/{设备ID}/telemetry/latest`
- 返回该设备每个属性的最新一条遥测记录（温度、湿度等），按时间倒序排列
- 用于前端快速获取设备当前实时数据，无需逐个属性查询

**2. 新增告警历史列表接口**
- 新增接口 `GET /api/v1/alerts/`，返回告警事件历史记录
- 支持分页查询（page、page_size 参数）
- 支持按状态（status）、设备（device_id）、严重级别（severity）筛选
- 与原有 `/alerts/rules`（告警规则）、`/alerts/events`（告警事件）接口并存

**3. 修复产品属性创建接口500错误**
- 修复 `POST /api/v1/products/{产品Key}/properties` 在 access_type 传入 `rw`/`ro` 等常见简写时报500错误的问题
- 系统现自动将 `rw`、`wr`、`w`、`write` 等归一化为 `read_write`，将 `r`、`ro`、`read` 等归一化为 `read_only`
- 兼容原有 `read_only`/`read_write` 标准值，不影响已存在的属性数据

**4. 修复admin用户角色显示错误**
- 修复登录后顶部导航栏显示"admin 查看员"而非"admin 管理员"的问题
- 根因：`/auth/me` API返回用户信息时未携带role字段

**5. 修复设备管理统计卡片显示为0**
- 修复设备管理页面4个统计卡片（总设备/在线/离线/异常）始终显示0的问题
- 根因：后端路由顺序错误，`/status-summary` 被 `/{device_id}` 动态路由拦截

**6. 修复场景联动设备下拉框显示数字ID**
- 修复创建场景时"选择设备"下拉框只显示数字ID（如1,2,3）而非设备名称的问题
- 现在正确显示设备名称（如"大棚01-已更新"）

**7. 物模型编辑器删除操作增加确认对话框**
- 删除属性、服务、事件时，系统现在会弹出确认对话框
- 提示"删除后需点击保存更改生效"，防止用户误删
- 符合农户"傻瓜式操作、可撤销"的产品设计原则

**8. 修复3D数字孪生场景animationId未定义错误**
- 修复3D场景初始化时`animationId is not defined`的ReferenceError
- 根因：`animationId`变量在`animate()`函数中使用但未提前声明
- 修复：在变量声明区域添加`let animationId = null`

**9. 设备模拟器数据产出验证**
- 模拟器在后端启动时自动运行，无需手动启动
- 每10秒为所有在线设备（5台）生成9项传感器遥测数据
- 传感器列表：temperature、humidity、light_intensity、soil_moisture、co2、soil_temperature、soil_ph、wind_speed、rainfall
- 数据写入Telemetry表，同步触发告警检查和场景联动
- 截至测试时数据库已累积42万+条遥测记录

**10. 修复用户管理角色下拉选项显示冗余描述**
- 修复角色下拉选项显示"管理员 - 拥有全部权限…"等冗长文本
- 修改el-option的label从`r.name + ' - ' + r.description`改为`r.name`
- 下拉现在简洁显示：管理员、操作员、查看员

#### 操作说明

**查看新增传感器数据：**
1. 进入"仪表盘"页面，在"环境实时监测"区域可看到全部9项传感器数据
2. 点击"环境趋势"区域的单选按钮，可切换查看不同参数的历史趋势
3. 进入"数字孪生"大屏，左侧环境监测面板展示全部9项实时数据
4. 进入"数据大屏"，左侧面板以卡片形式展示全部9项环境数据及进度条
5. 进入"传感器监控"页面，可查看所有传感器的实时值、趋势图和统计数据

**控制新增设备：**
1. 进入"数字孪生"大屏
2. 在右侧"设备控制"区域找到遮阳帘、电磁阀、加热膜开关
3. 点击开关即可控制设备启停，底部会显示"命令已下发"提示

**配置新传感器告警规则：**
1. 进入"告警规则"页面
2. 点击"新增规则"按钮
3. 选择设备后，在"监测属性"下拉框中可选择土壤pH值、风速、雨量
4. 设置阈值条件（大于/小于/等于等）和告警级别
5. 点击确定保存规则

**配置新传感器场景联动：**
1. 进入"场景联动"页面
2. 点击"创建场景"按钮
3. 触发类型选择"阈值触发"
4. 在"监测指标"下拉框中可选择土壤pH值、风速、雨量
5. 设置触发条件和阈值
6. 配置触发后要执行的动作（如开启/关闭设备）
7. 点击保存完成场景创建

**预设自动化场景列表：**

系统内置了8个自动化场景，无需配置即可自动运行：

| 场景名称 | 触发条件 | 执行动作 |
|---------|---------|---------|
| 高温自动通风 | 温度 > 30°C | 打开通风扇 |
| 低温自动保温 | 温度 < 15°C | 关闭通风扇 |
| 干旱自动灌溉 | 土壤湿度 < 30% | 打开灌溉水泵 |
| 雨天自动关通风扇 | 雨量 > 0.5mm | 关闭通风扇 |
| 大风自动关遮阳帘 | 风速 > 10m/s | 关闭遮阳帘 |
| 土壤pH异常告警 | 土壤pH < 5.5 | 触发告警通知 |
| 土壤pH过高告警 | 土壤pH > 7.5 | 触发告警通知 |
| 测试高温场景 | 温度 > 35°C | 打开通风扇 |

**场景自动执行机制：**
- 系统每30秒自动检查所有已启用的场景
- 当传感器数据满足触发条件时，自动执行预设动作
- 每个场景有冷却时间（60-300秒），避免频繁触发
- 场景执行结果可在"场景联动"页面查看执行日志
- 命令下发记录可在"命令下发"页面查看详情

**使用可视化物模型编辑器：**
1. 确保系统处于"高级模式"（顶部右侧可切换）
2. 在左侧菜单找到"物模型编辑器"，点击进入
3. 在页面顶部下拉框中选择要编辑的产品
4. 左侧"快捷添加"区域提供常用传感器和执行器模板，点击即可一键添加
5. 中间区域分三个标签页：属性、服务、事件
6. 点击每个条目的"编辑"按钮可修改参数，点击"删除"按钮可移除
7. 点击"新增属性/服务/事件"按钮可手动添加自定义项
8. 所有修改完成后，点击右上角"保存更改"按钮，系统会批量保存所有修改

**使用模板市场创建产品：**
1. 确保系统处于"高级模式"（顶部右侧可切换）
2. 在左侧菜单找到"模板市场"，点击进入
3. 浏览模板列表，可通过顶部的"分类"和"级别"下拉框筛选
4. 每个模板卡片展示：图标、名称、描述、传感器数量、执行器数量、告警规则数、自动化场景数、标签
5. 点击"预览物模型"可查看模板包含的核心传感器
6. 选择合适的模板后，点击"使用模板"按钮
7. 系统自动创建完整的产品，包含物模型（属性/服务/事件）、告警规则和自动化场景
8. 创建完成后自动跳转到产品管理页面，可查看新创建的产品

---

## 附录

### 常用操作入口速查表

| 功能需求 | 操作路径 |
|----------|----------|
| 登录系统 | 输入账号密码，点击登录 |
| 查看农场数据 | 仪表盘 |
| 添加设备 | 设备管理 > 新增设备 |
| 创建场景 | 场景联动 > 创建场景 |
| 查看告警 | 告警事件 |
| 控制设备 | 命令下发 > 下发命令 |
| 导出数据 | 遥测数据 > 导出 |
| 管理用户 | 用户管理（仅管理员） |
| 修改个人信息 | 用户头像 > 个人中心 |
| 编辑物模型 | 物模型编辑器（高级模式） |
| 从模板创建产品 | 模板市场（高级模式） |
| 修改密码 | 用户头像 > 个人中心 > 修改密码 |
| 3D数字孪生 | 左侧菜单 > 数字孪生 |
| 数据大屏 | 左侧菜单 > 数据大屏 |

### 状态指示灯说明

| 图标 | 含义 |
|------|------|
| 🟢 | 在线/正常 |
| 🔴 | 离线/异常 |
| 🟡 | 告警/警告 |
| 🔵 | 信息/提示 |
| ⚪ | 未知/未定义 |

### 常见问题解答

**Q: 设备显示离线怎么办？**
A: 检查设备是否通电、网络连接是否正常、SIM卡是否欠费。

**Q: 命令下发成功但设备没反应？**
A: 确认设备是否支持该命令，检查设备是否在线，查看命令记录详情。

**Q: 无法登录系统？**
A: 检查账号密码是否正确，联系管理员确认账号状态。

**Q: 数据没有更新？**
A: 确认设备是否正常上报数据，检查网络连接，尝试刷新页面。

---

## 第10章 授权管理（License）

> 本章面向系统发行方与学校管理员，说明如何为每个学校生成授权文件并部署。

### 10.1 授权机制概述

本平台采用 **RSA-2048 非对称签名 + 本地加密状态记录** 的离线授权方案，特点如下：

- **无需联网**：学校服务器无需连接外网即可完成授权校验
- **防伪造**：授权文件由发行方私钥签名，学校仅有公钥，无法自行生成授权
- **防时间回拨**：本地加密记录上次运行时间，若系统时间被恶意回拨，平台将锁定
- **过期只读**：授权过期后系统不会宕机，但会进入**只读模式**（可查看数据，不可新增/控制），方便学校续费过渡

### 10.2 为某学校生成授权文件

#### 首次使用前：生成密钥对

进入后端目录执行一次（仅需执行一次）：

```bash
cd backend
python scripts/generate_license.py --school "测试学校" --days 30 --devices 50
```

执行后会自动生成：
- `backend/keys/.private` —— **私钥（务必保管好，切勿泄露）**
- `backend/keys/.public` —— 公钥

#### 生成学校授权

**方式一：按有效期（月/天）**

```bash
# 授权 6 个月，最多 100 台设备
python scripts/generate_license.py --school "某某职业技术学院" --months 6 --devices 100

# 授权 14 天，最多 10 台设备
python scripts/generate_license.py --school "测试学校" --days 14 --devices 10
```

**方式二：指定过期日期**

```bash
# 授权到 2027-01-15，最多 50 台设备
python scripts/generate_license.py --school "XX大学" --date 2027-01-15 --devices 50
```

生成成功后，文件位于 `backend/licenses/` 目录下：
- `某某职业技术学院_20270115.policy` —— 授权文件
- `某某职业技术学院_deploy_public.key` —— 公钥文件

### 10.3 部署到学校服务器

将以下两个文件放到学校服务器的 `backend/` 目录下，并重命名：

| 原始文件 | 部署后文件名 | 存放路径 |
|---------|------------|---------|
| `某某学校.policy` | `.policy` | `backend/.policy` |
| `某某学校_deploy_public.key` | `.auth` | `backend/.auth` |

完成后重启后端服务即可生效：

```bash
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000
```

### 10.4 授权状态说明

| 状态 | 表现 | 处理建议 |
|------|------|---------|
| 授权有效 | 所有功能正常使用 | 无需处理 |
| 授权过期（只读模式） | 可查看数据，但新增设备、下发命令、修改配置等写操作被拦截，页面会提示"授权已过期，当前仅支持只读访问" | 联系发行方续期，替换新的 `.policy` 文件后重启 |
| 授权已失效 | 系统拒绝启动，日志提示"授权校验失败" | 检查 `.policy` 和 `.auth` 文件是否存在、是否匹配 |
| 授权文件损坏 | 系统拒绝启动 | 重新向发行方申请授权文件 |

### 10.5 防破解说明

本授权机制包含以下防护措施：

1. **RSA 数字签名**：授权内容经私钥签名，篡改任何字段都会导致验签失败
2. **加密运行状态**：系统在本地创建 `.state` 文件，加密记录上次运行时间
3. **时间回拨检测**：若系统时间被回拨到上次运行时间之前，平台将锁定
4. **状态文件防删**：授权签发 7 天后若 `.state` 文件被删除，系统视为异常并拒绝启动
5. **过期只读不降权**：过期后保留数据查看权限，降低学校对抗动机，促进续费

### 10.6 常见问题

**Q: 学校服务器重装系统后授权还有效吗？**
A: 只要 `.policy` 和 `.auth` 文件还在，复制回 `backend/` 目录即可。若 `.state` 丢失且已超过签发 7 天，请联系发行方重新签发授权。

**Q: 可以修改授权文件里的设备数量或过期时间吗？**
A: 不可以。任何修改都会导致 RSA 签名验证失败，系统拒绝启动。

**Q: 学校把系统时间改到授权期内能继续使用吗？**
A: 不能。`.state` 文件会记录真实运行时间，回拨检测会触发锁定。

## 第11章 设备模拟器

> 模拟器用于在无真实硬件设备的情况下，模拟传感器数据上报，方便测试和演示。

### 11.1 模拟器概述

模拟器在后端服务启动时**自动运行**，每 3 秒更新一次所有在线设备的传感器数据。

**支持的传感器（共 9 项）：**

| 传感器 | 属性标识 | 默认值 | 范围 | 变化幅度 |
|--------|---------|--------|------|---------|
| 空气温度 | `temperature` | 25.0°C | 15.0 ~ 35.0°C | ±0.5°C |
| 空气湿度 | `humidity` | 55.0% | 30.0 ~ 90.0% | ±1.0% |
| 土壤湿度 | `soil_moisture` | 45.0% | 20.0 ~ 90.0% | ±0.8% |
| 光照强度 | `light_intensity` | 5000 lux | 500 ~ 80000 lux | ±500 lux |
| CO₂浓度 | `co2` | 800 ppm | 300 ~ 2000 ppm | ±50 ppm |
| 土壤温度 | `soil_temperature` | 22.0°C | 10.0 ~ 35.0°C | ±0.3°C |
| 土壤 pH 值 | `soil_ph` | 6.5 | 4.0 ~ 9.0 | ±0.05 |
| 风速 | `wind_speed` | 2.0 m/s | 0.0 ~ 15.0 m/s | ±0.3 m/s |
| 雨量 | `rainfall` | 0.0 mm | 0.0 ~ 20.0 mm | ±0.2 mm |

### 11.2 配置参数

修改 `backend/.env` 文件：

```bash
# 模拟器更新间隔（秒），默认 3
SIMULATOR_INTERVAL=3
```

### 11.3 工作机制

1. **自动启动**：后端服务启动时自动初始化并运行模拟器
2. **仅模拟在线设备**：只更新 `status == "online"` 的设备
3. **数据变化**：每次更新在当前值基础上随机增减，不超出传感器范围
4. **联动触发**：每次更新后自动检查告警规则和场景联动条件
5. **实时推送**：通过 SSE 向前端推送实时数据

### 11.4 手动控制（可选）

模拟器目前由后端自动管理，如需手动控制，可以在代码中调用：

```python
# 启动模拟器（后端启动时自动调用）
await simulator_service.start(async_session_maker)

# 停止模拟器
await simulator_service.stop()
```

### 11.5 注意事项

- 模拟器仅用于测试和演示，生产环境中应连接真实设备
- 模拟器会产生大量遥测数据，测试完成后建议清理
- 模拟器运行不影响真实设备的数据上报

---

### 10.7 授权生成工具使用方法（发行方）

#### 工具位置
```
backend/scripts/generate_license.py
```

#### 参数说明

| 参数 | 必填 | 说明 |
|------|------|------|
| `--school` | 是 | 学校名称 |
| `--months` | 二选一 | 授权时长（月），如 `6` |
| `--days` | 二选一 | 授权时长（天），如 `14` |
| `--date` | 二选一 | 过期日期（YYYY-MM-DD），优先级最高 |
| `--devices` | 否 | 设备上限，默认 100 |
| `--out` | 否 | 自定义输出文件名 |

#### 使用示例

```bash
# 授权 6 个月，最多 100 台设备
python scripts/generate_license.py --school "某某职业技术学院" --months 6 --devices 100

# 授权到指定日期
python scripts/generate_license.py --school "XX大学" --date 2027-06-30 --devices 50

# 短期测试授权
python scripts/generate_license.py --school "测试学校" --days 7 --devices 10
```

#### 输出文件

生成后位于 `backend/licenses/` 目录：
- `某某学校_YYYYMMDD.policy` — 授权文件（交付给学校，重命名为 `.policy`）
- `某某学校_deploy_public.key` — 公钥文件（交付给学校，重命名为 `.auth`）

#### 详细手册

完整的使用说明请参考：
```
backend/scripts/LICENSE_TOOL_MANUAL.md
```

---

*本手册基于智慧农业物联网平台编写，如有更新恕不另行通知。*
*版权所有 © 2024 智慧农业物联网平台*
