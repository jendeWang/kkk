# 智慧大棚物联网平台 - 国赛测试方案

> **版本**: v1.0
> **日期**: 2026-06-30
> **角色**: 4号 - 测试工程师（质量把关者）
> **目标**: 冲击国一

---

## 目录

1. [测试策略设计](#一测试策略设计)
2. [重点模块测试用例](#二重点模块测试用例)
3. [比赛现场风险防控](#三比赛现场风险防控)
4. [自动化测试方案](#四自动化测试方案)
5. [测试执行计划](#五测试执行计划)

---

## 一、测试策略设计

### 1.1 测试总体策略

**核心原则**：以演示效果为导向，以稳定性为底线，以亮点功能为加分项。

| 测试维度 | 优先级 | 目标 | 投入占比 |
|---------|--------|------|---------|
| 功能测试 | P0 | 核心功能100%通过，亮点功能零Bug | 30% |
| 性能测试 | P1 | 关键操作<200ms，大屏流畅无卡顿 | 15% |
| 稳定性测试 | P0 | 连续运行72小时无崩溃 | 20% |
| 展示效果测试 | P0 | 视觉效果达到演示级标准 | 15% |
| 演示脚本测试 | P0 | 完整演示流程零失误 | 15% |
| 应急演练测试 | P1 | 常见故障30秒内恢复 | 5% |

### 1.2 功能测试策略

#### 1.2.1 测试范围分级

| 级别 | 模块 | 质量标准 | 缺陷容忍度 |
|------|------|---------|-----------|
| P0（致命） | 登录、设备管理、数据展示、命令下发、大屏 | 100%通过 | 0个 |
| P1（重要） | 告警规则、场景联动、沙盘拓扑、AI预测 | 95%通过 | ≤2个轻微 |
| P2（一般） | 用户管理、API密钥、操作日志 | 90%通过 | ≤3个轻微 |

#### 1.2.2 测试方法

- **黑盒测试**：基于需求文档验证功能正确性
- **边界值测试**：传感器数据上下限、告警阈值边界
- **异常测试**：网络异常、数据异常、操作异常
- **回归测试**：每个版本发布前全量回归

### 1.3 性能测试策略

#### 1.3.1 性能指标

| 指标 | 目标值 | 测试方法 |
|------|--------|---------|
| API响应时间 | P95 < 200ms | JMeter压测 |
| 页面首屏加载 | < 2s | Lighthouse |
| 大屏渲染帧率 | ≥ 30fps | Chrome DevTools |
| MQTT消息处理 | ≥ 100条/秒 | 压测脚本 |
| 并发用户数 | ≥ 50人 | 并发测试 |

#### 1.3.2 性能测试场景

1. **设备接入性能**：模拟1000台设备同时上报数据
2. **查询性能**：查询100万条遥测数据的响应时间
3. **告警触发性能**：同时触发100条告警的处理能力
4. **大屏渲染性能**：大量数据点的图表渲染流畅度

### 1.4 稳定性测试策略

#### 1.4.1 稳定性测试类型

| 测试类型 | 时长 | 目标 |
|---------|------|------|
| 冒烟测试 | 30分钟 | 基本功能正常 |
| 隔夜测试 | 12小时 | 夜间无人工干预运行 |
| 72小时耐久测试 | 72小时 | 连续运行无崩溃 |
| 压力稳定性 | 4小时 | 高负载下稳定运行 |

#### 1.4.2 稳定性监控指标

- 内存使用率（无内存泄漏）
- CPU使用率（稳定在合理范围）
- 服务可用性（>99.9%）
- 错误日志数量（0条新增错误）
- 消息丢失率（<0.1%）

### 1.5 展示效果测试策略

#### 1.5.1 视觉效果检查清单

| 检查项 | 标准 |
|--------|------|
| 大屏配色 | 科技感、对比度适中、文字清晰 |
| 图表动效 | 过渡流畅、无卡顿、无闪烁 |
| 3D效果（如有） | 视角合理、模型精细、交互流畅 |
| 数据实时性 | 数据更新有视觉反馈 |
| 响应式布局 | 不同分辨率下无错位 |

#### 1.5.2 演示环境适配

- **分辨率测试**：1920×1080、2560×1440、4K
- **浏览器兼容**：Chrome、Edge、Firefox最新版
- **投影仪适配**：颜色校准、亮度适配

### 1.6 演示脚本测试策略

#### 1.6.1 演示脚本分级

| 级别 | 脚本 | 时长 | 适用场景 |
|------|------|------|---------|
| A版（完整版） | 全功能演示 | 8-10分钟 | 决赛现场 |
| B版（精简版） | 核心亮点演示 | 3-5分钟 | 初选/快速展示 |
| C版（应急版） | 最小可用演示 | 2分钟 | 故障应急 |

#### 1.6.2 演示脚本测试要点

1. **时间控制**：每步操作时间精确到秒
2. **衔接流畅**：页面切换、数据加载无缝衔接
3. **容错设计**：每步都有备选方案
4. **话术配合**：操作与讲解同步

### 1.7 应急演练测试策略

| 演练场景 | 恢复目标 | 负责人 |
|---------|---------|--------|
| 网络中断 | 30秒切换到本地模式 | 1人 |
| 硬件故障 | 1分钟切换到模拟数据 | 1人 |
| 页面崩溃 | 30秒刷新恢复 | 1人 |
| 数据异常 | 1分钟切换备用数据源 | 1人 |
| 演示失误 | 立即跳转到下一个亮点 | 主讲人 |

---

## 二、重点模块测试用例

### 2.1 硬件沙盘功能测试

#### TC-SAND-001：传感器数据上报验证
- **前置条件**：沙盘设备已通电，MQTT连接正常
- **操作步骤**：
  1. 查看沙盘上温度传感器当前读数
  2. 在系统遥测数据页查看对应设备的温度值
  3. 用手握住传感器改变温度
  4. 观察系统数据是否同步变化
- **预期结果**：
  - 数据上报延迟 < 3秒
  - 数值误差 ±0.5°C 以内
  - 数据变化趋势一致
- **优先级**：P0

#### TC-SAND-002：执行器远程控制测试
- **前置条件**：执行器设备在线
- **操作步骤**：
  1. 在系统中选择"水帘"执行器
  2. 点击"开启"按钮
  3. 观察沙盘上水帘是否真的启动
  4. 点击"关闭"按钮
  5. 观察水帘是否停止
- **预期结果**：
  - 命令下发响应 < 2秒
  - 执行器动作准确
  - 状态反馈正确显示
- **优先级**：P0

#### TC-SAND-003：设备上下线状态同步
- **前置条件**：设备在线
- **操作步骤**：
  1. 断开某台设备的电源
  2. 观察系统中设备状态是否变为离线
  3. 重新接通电源
  4. 观察设备状态是否恢复在线
- **预期结果**：
  - 离线检测 < 30秒
  - 上线恢复 < 5秒
  - 状态变更有日志记录
- **优先级**：P0

#### TC-SAND-004：多传感器同时上报
- **前置条件**：所有传感器正常工作
- **操作步骤**：
  1. 同时触发多个传感器数据变化
  2. 观察系统数据接收情况
  3. 检查数据是否有丢失
- **预期结果**：
  - 所有数据完整接收
  - 数据顺序正确
  - 无消息丢失
- **优先级**：P1

#### TC-SAND-005：边缘网关离线缓存
- **前置条件**：边缘网关正常运行
- **操作步骤**：
  1. 断开网关与云平台的网络连接
  2. 传感器继续上报数据
  3. 5分钟后恢复网络
  4. 检查离线期间数据是否补传
- **预期结果**：
  - 离线期间数据本地缓存
  - 网络恢复后自动补传
  - 数据完整无丢失
- **优先级**：P1

#### TC-SAND-006：沙盘点灯效果测试
- **前置条件**：沙盘灯光系统正常
- **操作步骤**：
  1. 在系统中切换不同场景模式
  2. 观察沙盘灯光变化
  3. 检查灯光颜色、亮度是否符合预期
- **预期结果**：
  - 灯光变化响应 < 1秒
  - 颜色准确无误
  - 各区域灯光独立控制
- **优先级**：P1

#### TC-SAND-007：设备故障告警联动
- **前置条件**：告警规则已配置
- **操作步骤**：
  1. 模拟传感器数据异常（超阈值）
  2. 观察系统是否产生告警
  3. 观察沙盘告警灯是否亮起
- **预期结果**：
  - 告警触发 < 5秒
  - 沙盘告警灯同步亮起
  - 告警信息准确
- **优先级**：P0

#### TC-SAND-008：沙盘与大屏联动
- **前置条件**：大屏和沙盘都正常运行
- **操作步骤**：
  1. 在沙盘上操作某个按钮
  2. 观察大屏数据是否同步更新
  3. 在大屏上执行操作
  4. 观察沙盘是否有反馈
- **预期结果**：
  - 双向联动延迟 < 2秒
  - 数据状态一致
  - 交互反馈明显
- **优先级**：P0

### 2.2 AI预测功能测试

#### TC-AI-001：病虫害预测准确率
- **前置条件**：有历史病虫害数据
- **操作步骤**：
  1. 输入10组已知结果的环境数据
  2. 触发AI预测
  3. 对比预测结果与实际结果
  4. 计算准确率
- **预期结果**：
  - 预测准确率 ≥ 85%
  - 预测响应时间 < 5秒
  - 给出置信度和建议
- **优先级**：P1

#### TC-AI-002：产量预测合理性
- **前置条件**：有历史产量数据
- **操作步骤**：
  1. 选择不同作物生长阶段
  2. 查看产量预测结果
  3. 验证预测值是否在合理范围
  4. 调整环境参数，观察预测变化
- **预期结果**：
  - 预测值在合理区间
  - 参数调整后预测趋势正确
  - 显示预测曲线图
- **优先级**：P1

#### TC-AI-003：生长周期预测
- **前置条件**：作物生长模型已建立
- **操作步骤**：
  1. 输入播种日期和当前环境数据
  2. 查看各生长阶段预测时间
  3. 与实际农业经验对比
- **预期结果**：
  - 各阶段时间预测合理
  - 显示生长进度条
  - 给出采收建议时间
- **优先级**：P1

#### TC-AI-004：异常输入处理
- **前置条件**：AI预测功能正常
- **操作步骤**：
  1. 输入缺失部分参数的数据
  2. 输入明显异常的数据（如温度100°C）
  3. 输入空数据
  4. 观察系统处理方式
- **预期结果**：
  - 缺失参数有默认值或提示
  - 异常数据有校验提示
  - 系统不崩溃、不报错
- **优先级**：P1

#### TC-AI-005：预测结果可视化
- **前置条件**：有预测结果
- **操作步骤**：
  1. 查看预测结果页面
  2. 检查图表展示效果
  3. 验证数据与图表一致性
- **预期结果**：
  - 图表清晰美观
  - 数据标注准确
  - 交互操作流畅
- **优先级**：P2

#### TC-AI-006：历史预测回溯
- **前置条件**：有历史预测记录
- **操作步骤**：
  1. 查看历史预测记录
  2. 对比预测值与实际值
  3. 验证准确率计算
- **预期结果**：
  - 历史记录完整
  - 准确率统计正确
  - 可按时间筛选
- **优先级**：P2

### 2.3 大屏展示测试

#### TC-SCREEN-001：大屏整体布局
- **前置条件**：大屏页面可访问
- **操作步骤**：
  1. 打开大屏页面
  2. 检查整体布局结构
  3. 验证各模块位置和尺寸
- **预期结果**：
  - 布局对称、协调
  - 各模块比例合理
  - 无错位、无重叠
- **优先级**：P0

#### TC-SCREEN-002：数据实时刷新
- **前置条件**：有设备在上报数据
- **操作步骤**：
  1. 观察大屏数据变化
  2. 记录刷新频率
  3. 验证数据与后台一致性
- **预期结果**：
  - 数据每3秒自动刷新
  - 刷新有过渡动画
  - 数据与后台一致
- **优先级**：P0

#### TC-SCREEN-003：环境监测面板
- **前置条件**：有环境传感器数据
- **操作步骤**：
  1. 查看温度、湿度、光照等面板
  2. 检查数值显示
  3. 检查仪表盘/进度条动效
- **预期结果**：
  - 数值清晰易读
  - 单位标注正确
  - 动效流畅自然
- **优先级**：P0

#### TC-SCREEN-004：设备状态地图
- **前置条件**：有多个设备分布
- **操作步骤**：
  1. 查看设备分布地图
  2. 检查在线/离线设备标识
  3. 点击设备查看详情
- **预期结果**：
  - 设备位置标注准确
  - 状态颜色区分明显
  - 点击弹出详情正确
- **优先级**：P0

#### TC-SCREEN-005：趋势图表展示
- **前置条件**：有历史数据
- **操作步骤**：
  1. 查看24小时趋势图
  2. 检查X轴Y轴标注
  3. 验证数据曲线正确性
- **预期结果**：
  - 图表渲染流畅
  - 坐标轴标注清晰
  - 数据曲线准确
- **优先级**：P1

#### TC-SCREEN-006：告警弹窗展示
- **前置条件**：配置了告警规则
- **操作步骤**：
  1. 触发一条告警
  2. 观察大屏告警提示
  3. 检查告警信息展示
- **预期结果**：
  - 告警弹窗及时出现
  - 告警级别颜色区分
  - 动画效果醒目
- **优先级**：P0

#### TC-SCREEN-007：全屏展示效果
- **前置条件**：大屏页面正常
- **操作步骤**：
  1. 按F11进入全屏
  2. 检查全屏显示效果
  3. 验证各元素适配情况
- **预期结果**：
  - 全屏无黑边
  - 元素比例正常
  - 无横向/纵向滚动条
- **优先级**：P0

#### TC-SCREEN-008：3D数字孪生（如有）
- **前置条件**：3D模型已加载
- **操作步骤**：
  1. 查看3D大棚模型
  2. 旋转、缩放视角
  3. 检查模型细节
- **预期结果**：
  - 模型加载 < 5秒
  - 旋转缩放流畅
  - 细节清晰可辨
- **优先级**：P1

### 2.4 场景联动测试

#### TC-SCENE-001：自动通风场景
- **前置条件**：
  - 温度传感器在线
  - 通风设备在线
  - 已配置"温度>30°C自动通风"场景
- **操作步骤**：
  1. 模拟温度上升到30°C以上
  2. 观察通风设备是否自动开启
  3. 模拟温度下降到28°C以下
  4. 观察通风设备是否自动关闭
- **预期结果**：
  - 触发条件满足后5秒内执行
  - 设备动作准确
  - 有场景执行日志
- **优先级**：P0

#### TC-SCENE-002：多条件复合场景
- **前置条件**：
  - 温度、湿度传感器在线
  - 喷淋设备在线
  - 配置"温度>32°C且湿度<40%时启动喷淋"
- **操作步骤**：
  1. 只满足温度条件（湿度正常）
  2. 验证喷淋不启动
  3. 同时满足温度和湿度条件
  4. 验证喷淋启动
- **预期结果**：
  - 单条件不触发
  - 所有条件满足才触发
  - 逻辑判断正确
- **优先级**：P0

#### TC-SCENE-003：场景手动触发
- **前置条件**：已创建多个场景
- **操作步骤**：
  1. 在场景列表中选择"快速降温"场景
  2. 点击"立即执行"
  3. 观察各设备动作
  4. 验证执行结果
- **预期结果**：
  - 场景立即执行
  - 所有关联设备按顺序动作
  - 显示执行进度
- **优先级**：P1

#### TC-SCENE-004：定时场景执行
- **前置条件**：配置了"早上6点开灯"定时场景
- **操作步骤**：
  1. 修改系统时间到5:59
  2. 等待到6:00
  3. 观察灯光是否自动开启
  4. 检查执行记录
- **预期结果**：
  - 准点触发（误差<30秒）
  - 设备动作正确
  - 有执行日志
- **优先级**：P1

#### TC-SCENE-005：场景联动告警
- **前置条件**：配置了告警联动场景
- **操作步骤**：
  1. 触发一条严重告警
  2. 观察是否启动应急场景
  3. 检查应急设备动作
- **预期结果**：
  - 告警触发后立即启动应急场景
  - 应急设备按预案动作
  - 告警与场景关联记录
- **优先级**：P1

#### TC-SCENE-006：场景执行失败处理
- **前置条件**：场景中的某台设备离线
- **操作步骤**：
  1. 让场景中的一台设备离线
  2. 触发场景执行
  3. 观察系统处理方式
  4. 检查错误提示
- **预期结果**：
  - 其他正常设备继续执行
  - 失败设备有明确提示
  - 记录失败原因
- **优先级**：P1

#### TC-SCENE-007：场景优先级
- **前置条件**：配置了两个有冲突的场景
- **操作步骤**：
  1. 同时触发两个场景
  2. 观察哪个场景优先生效
  3. 验证优先级设置
- **预期结果**：
  - 优先级高的场景执行
  - 优先级低的场景排队或跳过
  - 有冲突提示
- **优先级**：P2

### 2.5 演示流程测试

#### TC-DEMO-001：完整版演示脚本（A版）
- **演示时长**：8-10分钟
- **操作步骤**：

  | 步骤 | 操作 | 讲解要点 | 时长 |
  |------|------|---------|------|
  | 1 | 开场，展示大屏首页 | 项目背景、整体架构 | 30秒 |
  | 2 | 切换到设备总览 | 设备接入规模、覆盖范围 | 45秒 |
  | 3 | 展示实时环境监测 | 多维度数据采集、精度 | 1分钟 |
  | 4 | 演示沙盘联动 | 硬件沙盘+数字孪生 | 1.5分钟 |
  | 5 | 触发告警场景 | 智能告警、自动处置 | 1分钟 |
  | 6 | 展示AI预测 | 病虫害/产量/生长周期 | 1.5分钟 |
  | 7 | 演示场景联动 | 全自动智能管控 | 1分钟 |
  | 8 | 展示移动端/小程序 | 随时随地管控 | 45秒 |
  | 9 | 总结收尾 | 技术亮点、应用价值 | 30秒 |

- **预期结果**：
  - 全程无卡顿、无报错
  - 各功能切换流畅
  - 时间控制在10分钟内
- **优先级**：P0

#### TC-DEMO-002：精简版演示脚本（B版）
- **演示时长**：3-5分钟
- **操作步骤**：

  | 步骤 | 操作 | 亮点 | 时长 |
  |------|------|------|------|
  | 1 | 大屏开场 | 视觉冲击 | 20秒 |
  | 2 | 沙盘联动 | 实物+数字 | 1分钟 |
  | 3 | AI预测 | 技术含量 | 1分钟 |
  | 4 | 场景联动 | 自动化 | 1分钟 |
  | 5 | 收尾 | 总结升华 | 20秒 |

- **预期结果**：
  - 亮点突出
  - 节奏紧凑
  - 5分钟内完成
- **优先级**：P0

#### TC-DEMO-003：演示容错能力
- **前置条件**：正在进行演示
- **操作步骤**：
  1. 模拟某一步操作失败
  2. 测试备选方案是否可用
  3. 测试能否快速跳过到下一步
- **预期结果**：
  - 每步都有备选方案
  - 切换时间 < 10秒
  - 不影响整体演示节奏
- **优先级**：P0

#### TC-DEMO-004：评委交互应对
- **前置条件**：演示结束
- **操作步骤**：
  1. 模拟评委要求查看某个细节
  2. 测试能否快速定位到对应页面
  3. 测试数据真实性验证
- **预期结果**：
  - 能快速响应评委要求
  - 数据真实可追溯
  - 操作熟练流畅
- **优先级**：P1

#### TC-DEMO-005：网络异常下的演示
- **前置条件**：正在演示
- **操作步骤**：
  1. 突然断开网络
  2. 测试本地缓存数据是否可用
  3. 测试能否切换到离线演示模式
- **预期结果**：
  - 30秒内切换到离线模式
  - 演示可继续进行
  - 不暴露明显故障
- **优先级**：P0

---

## 三、比赛现场风险防控

### 3.1 风险等级矩阵

| 风险类别 | 发生概率 | 影响程度 | 风险等级 |
|---------|---------|---------|---------|
| 网络故障 | 中 | 致命 | P0 |
| 硬件故障 | 中 | 严重 | P0 |
| 软件Bug | 低 | 严重 | P1 |
| 演示失误 | 中 | 一般 | P1 |
| 电源故障 | 低 | 致命 | P0 |
| 设备丢失/损坏 | 低 | 致命 | P0 |
| 评委提问卡壳 | 中 | 一般 | P2 |

### 3.2 应急预案详细

#### 3.2.1 网络中断应急预案

**触发条件**：网络连接断开、延迟过高、数据不更新

**应急步骤**：

| 时间 | 动作 | 负责人 | 操作细节 |
|------|------|--------|---------|
| 0-5秒 | 发现问题 | 操作手 | 发现数据不更新或网络报错 |
| 5-15秒 | 初步判断 | 操作手 | 快速检查网络连接状态 |
| 15-30秒 | 切换方案 | 操作手 | 切换到本地Mock模式 |
| 30秒后 | 继续演示 | 主讲人 | 按计划继续，不提网络问题 |

**具体操作**：
1. 按预设快捷键（如 Ctrl+Shift+M）切换到Mock数据模式
2. 前端提前内置完整的Mock数据集
3. 主讲人继续讲解，不提及故障
4. 后台人员排查网络问题

**准备工作**：
- 提前准备好完整的Mock数据
- 一键切换功能测试通过
- Mock数据与真实数据相似度>90%

#### 3.2.2 硬件故障应急预案

**触发条件**：沙盘设备不工作、传感器无数据、执行器无反应

**应急步骤**：

| 场景 | 应对方案 | 切换时间 |
|------|---------|---------|
| 单个传感器故障 | 跳过该传感器，用其他传感器演示 | <10秒 |
| 单个执行器故障 | 手动辅助演示，说明原理 | <15秒 |
| 网关离线 | 切换到纯软件演示模式 | <30秒 |
| 沙盘整体故障 | 直接用大屏+视频演示 | <1分钟 |

**备用方案**：
1. **方案A（轻微故障）**：继续使用沙盘，跳过故障部件
2. **方案B（中度故障）**：切换到数字孪生3D演示
3. **方案C（严重故障）**：直接播放预录演示视频

**准备工作**：
- 提前录制高质量演示视频
- 视频与现场讲解节奏匹配
- 视频播放器准备就绪

#### 3.2.3 软件Bug应急预案

**触发条件**：页面崩溃、功能异常、数据错乱

**应急步骤**：

| Bug类型 | 应对措施 | 负责人 |
|---------|---------|--------|
| 页面白屏/崩溃 | 立即刷新页面 | 操作手 |
| 功能点异常 | 快速跳过，演示下一个功能 | 主讲人 |
| 数据显示错误 | 说明是演示环境，快速切走 | 主讲人 |
| 系统完全卡死 | 切换到备用机演示 | 后勤 |

**关键操作**：
1. 刷新快捷键：F5
2. 浏览器设置：禁用缓存、开启硬件加速
3. 备用机：提前配置好，随时可以接上

**准备工作**：
- 两台电脑同时运行，一台主用一台备用
- KVM切换器或HDMI切换器
- 切换时间<5秒

#### 3.2.4 电源故障应急预案

**触发条件**：断电、电压不稳

**应对措施**：
1. **UPS不间断电源**：所有设备接UPS，续航≥30分钟
2. **笔记本电脑**：使用笔记本演示，自带电池续航
3. **移动电源**：沙盘设备可用移动电源供电

**准备工作**：
- UPS提前充满电并测试
- 笔记本电池100%
- 准备插线板和转换头

#### 3.2.5 评委提问应对预案

**常见问题分类及应对策略**：

| 问题类型 | 应对策略 | 示例话术 |
|---------|---------|---------|
| 技术细节 | 诚实回答，不懂就说后续研究 | "这个问题很专业，我们目前的方案是...，后续我们计划深入研究..." |
| 数据真实性 | 当场演示验证 | "评委老师您好，我可以现场给您演示一下真实数据..." |
| 创新点质疑 | 对比现有方案，突出差异化 | "和传统方案相比，我们的创新点在于..." |
| 应用价值 | 用数据说话，讲实际案例 | "根据我们的测算，这套系统可以..." |
| 答不上来 | 坦诚+转移 | "这个问题我们确实还在研究中，目前我们更关注..." |

**准备工作**：
- 整理50个常见问题及标准答案
- 技术细节全员过一遍
- 每人负责几个擅长的领域

### 3.3 赛前检查清单

#### 3.3.1 出发前检查（D-1）

| 检查项 | 状态 | 备注 |
|--------|------|------|
| 笔记本电脑×2 | ☐ | 充满电、装好软件 |
| 沙盘设备 | ☐ | 功能测试正常 |
| 电源线/插线板 | ☐ | 带够，带转换头 |
| 网线/WiFi热点 | ☐ | 备用网络 |
| UPS电源 | ☐ | 充满电 |
| HDMI线/转接器 | ☐ | 多种接口 |
| 演示视频U盘×2 | ☐ | 备份 |
| 纸质材料 | ☐ | 项目介绍、答辩材料 |
| 手机充电宝 | ☐ | |

#### 3.3.2 现场布置检查（D-Day 赛前1小时）

| 检查项 | 状态 | 备注 |
|--------|------|------|
| 设备通电正常 | ☐ | |
| 网络连接正常 | ☐ | 测一下速度 |
| 大屏显示正常 | ☐ | 分辨率适配 |
| 沙盘功能正常 | ☐ | 快速测一遍 |
| 音响设备正常 | ☐ | 麦克风测试 |
| 备用机就绪 | ☐ | 随时可切换 |
| 演示视频可用 | ☐ | |
| 人员到位 | ☐ | 分工明确 |

#### 3.3.3 临上场检查（赛前5分钟）

| 检查项 | 状态 | 备注 |
|--------|------|------|
| 关闭所有无关程序 | ☐ | 免打扰模式 |
| 关闭通知弹窗 | ☐ | 系统+软件通知 |
| 浏览器全屏 | ☐ | F11 |
| 电源已接通 | ☐ | 不使用电池 |
| 手机静音 | ☐ | |
| 水杯/杂物收好 | ☐ | 避免碰倒 |
| 深呼吸、微笑 | ☐ | 调整状态 |

### 3.4 人员分工与协作

#### 3.4.1 现场人员分工

| 角色 | 人数 | 职责 | 位置 |
|------|------|------|------|
| 主讲人 | 1人 | 讲解、答辩 | 台前 |
| 操作手 | 1人 | 电脑操作、切换 | 台侧 |
| 硬件保障 | 1人 | 沙盘设备、应急处理 | 台后 |
| 机动/后勤 | 1人 | 物资、跑腿、应急 | 台下 |

#### 3.4.2 沟通机制

- **手势暗语**：
  - 摸耳朵 = 慢点讲
  - 扶眼镜 = 加快进度
  - 摸鼻子 = 有问题，准备切换
  - 竖大拇指 = 一切正常

- **对讲机/耳机**：
  - 后台与操作手保持通讯
  - 主讲人戴隐形耳机（如允许）

---

## 四、自动化测试方案

### 4.1 自动化测试分层策略

```
┌─────────────────────────────────────┐
│         E2E 端到端测试 (10%)         │  ← 演示流程、关键业务流
├─────────────────────────────────────┤
│        接口层测试 (30%)              │  ← API接口、业务逻辑
├─────────────────────────────────────┤
│        单元测试 (60%)                │  ← 工具类、核心算法
└─────────────────────────────────────┘
```

### 4.2 接口自动化测试

#### 4.2.1 测试范围

| 模块 | 接口数量 | 自动化覆盖率 | 优先级 |
|------|---------|-------------|--------|
| 认证模块 | 2 | 100% | P0 |
| 设备管理 | 5 | 100% | P0 |
| 产品管理 | 16 | 80% | P1 |
| 遥测数据 | 3 | 100% | P0 |
| 命令下发 | 3 | 100% | P0 |
| 告警模块 | 8 | 90% | P0 |
| SSE推送 | 2 | 50% | P2 |

#### 4.2.2 技术方案

- **测试框架**：pytest + requests
- **测试报告**：Allure
- **数据驱动**：YAML/JSON 测试数据
- **持续集成**：GitHub Actions / Jenkins

#### 4.2.3 核心测试用例示例

```python
# test_api_demo.py

import pytest
import requests

BASE_URL = "http://localhost:8000/api/v1"

@pytest.fixture
def auth_token():
    """获取认证Token"""
    resp = requests.post(
        f"{BASE_URL}/auth/login",
        json={"username": "admin", "password": "admin123"}
    )
    return resp.json()["access_token"]

class TestDemoFlow:
    """演示流程接口测试"""
    
    def test_01_login(self):
        """测试登录"""
        resp = requests.post(
            f"{BASE_URL}/auth/login",
            json={"username": "admin", "password": "admin123"}
        )
        assert resp.status_code == 200
        assert "access_token" in resp.json()
    
    def test_02_dashboard_stats(self, auth_token):
        """测试仪表盘统计数据"""
        headers = {"Authorization": f"Bearer {auth_token}"}
        resp = requests.get(f"{BASE_URL}/dashboard/stats", headers=headers)
        assert resp.status_code == 200
        data = resp.json()
        assert "device_count" in data
        assert "online_count" in data
    
    def test_03_create_device(self, auth_token):
        """测试创建设备"""
        headers = {"Authorization": f"Bearer {auth_token}"}
        device_data = {
            "device_name": "测试设备",
            "product_id": 1
        }
        resp = requests.post(
            f"{BASE_URL}/devices/",
            json=device_data,
            headers=headers
        )
        assert resp.status_code == 200
        assert "device_key" in resp.json()
    
    def test_04_telemetry_query(self, auth_token):
        """测试遥测数据查询"""
        headers = {"Authorization": f"Bearer {auth_token}"}
        params = {"device_id": 1, "limit": 100}
        resp = requests.get(
            f"{BASE_URL}/telemetry/",
            params=params,
            headers=headers
        )
        assert resp.status_code == 200
```

#### 4.2.4 执行方式

```bash
# 运行所有测试
pytest tests/ -v --alluredir=allure-results

# 运行冒烟测试
pytest tests/ -m smoke

# 运行演示流程测试
pytest tests/ -m demo_flow

# 生成测试报告
allure serve allure-results
```

### 4.3 UI自动化测试

#### 4.3.1 测试范围

| 页面 | 自动化用例数 | 说明 |
|------|-------------|------|
| 登录页 | 3 | 正常登录、错误密码、空密码 |
| 仪表盘 | 2 | 数据展示、进入大屏 |
| 大屏页面 | 3 | 数据刷新、全屏展示、退出 |
| 设备管理 | 2 | 列表展示、详情查看 |
| 场景联动 | 2 | 场景列表、手动执行 |

#### 4.3.2 技术方案

- **测试框架**：Playwright
- **浏览器**：Chromium（Chrome）
- **录制工具**：Playwright Codegen

#### 4.3.3 演示流程自动化脚本示例

```javascript
// demo-flow.spec.js

const { test, expect } = require('@playwright/test');

test.describe('演示流程自动化测试', () => {
  
  test('完整演示流程', async ({ page }) => {
    // 1. 登录
    await page.goto('http://localhost:3000/login');
    await page.fill('input[placeholder="用户名"]', 'admin');
    await page.fill('input[placeholder="密码"]', 'admin123');
    await page.click('button:has-text("登录")');
    await expect(page).toHaveURL(/.*dashboard/);
    
    // 2. 仪表盘
    await page.waitForSelector('.dashboard-stats');
    await page.screenshot({ path: 'screenshots/01-dashboard.png' });
    
    // 3. 进入大屏
    await page.click('button:has-text("进入大屏")');
    await page.waitForSelector('.big-screen');
    await page.screenshot({ path: 'screenshots/02-bigscreen.png' });
    
    // 4. 等待数据刷新
    await page.waitForTimeout(3000);
    await page.screenshot({ path: 'screenshots/03-bigscreen-updated.png' });
    
    // 5. 退出大屏
    await page.click('button:has-text("退出大屏")');
    await expect(page).toHaveURL(/.*dashboard/);
    
    // 6. 设备管理
    await page.click('text=设备管理');
    await page.waitForSelector('.device-list');
    await page.screenshot({ path: 'screenshots/04-devices.png' });
    
    // 7. 告警管理
    await page.click('text=告警事件');
    await page.waitForSelector('.alert-list');
    await page.screenshot({ path: 'screenshots/05-alerts.png' });
  });
  
  test('大屏性能测试', async ({ page }) => {
    const metrics = [];
    
    await page.goto('http://localhost:3000/big-screen');
    
    // 收集10秒的性能数据
    for (let i = 0; i < 10; i++) {
      const metric = await page.evaluate(() => {
        return {
          fps: performance.getEntriesByType('paint').length,
          memory: performance.memory?.usedJSHeapSize
        };
      });
      metrics.push(metric);
      await page.waitForTimeout(1000);
    }
    
    console.log('性能数据:', metrics);
  });
});
```

### 4.4 性能自动化测试

#### 4.4.1 测试方案

- **工具**：k6（现代化负载测试工具）
- **场景**：
  - 冒烟测试：10个用户，1分钟
  - 负载测试：100个用户，5分钟
  - 压力测试：500个用户，10分钟
  - 稳定性测试：50个用户，1小时

#### 4.4.2 测试脚本示例

```javascript
// performance-test.js

import http from 'k6/http';
import { check, sleep } from 'k6';

export const options = {
  stages: [
    { duration: '30s', target: 50 },
    { duration: '2m', target: 50 },
    { duration: '30s', target: 0 },
  ],
  thresholds: {
    http_req_duration: ['p(95)<200'],
    http_req_failed: ['rate<0.01'],
  },
};

export default function () {
  // 登录
  const loginRes = http.post(
    'http://localhost:8000/api/v1/auth/login',
    JSON.stringify({ username: 'admin', password: 'admin123' }),
    { headers: { 'Content-Type': 'application/json' } }
  );
  check(loginRes, { '登录成功': (r) => r.status === 200 });
  
  const token = loginRes.json().access_token;
  const headers = { Authorization: `Bearer ${token}` };
  
  // 查询仪表盘
  const dashboardRes = http.get(
    'http://localhost:8000/api/v1/dashboard/stats',
    { headers }
  );
  check(dashboardRes, { '仪表盘查询成功': (r) => r.status === 200 });
  
  // 查询设备列表
  const devicesRes = http.get(
    'http://localhost:8000/api/v1/devices/?page=1&page_size=20',
    { headers }
  );
  check(devicesRes, { '设备列表查询成功': (r) => r.status === 200 });
  
  // 查询遥测数据
  const telemetryRes = http.get(
    'http://localhost:8000/api/v1/telemetry/?device_id=1&limit=100',
    { headers }
  );
  check(telemetryRes, { '遥测数据查询成功': (r) => r.status === 200 });
  
  sleep(1);
}
```

### 4.5 监控与告警自动化

#### 4.5.1 健康检查脚本

```bash
#!/bin/bash
# health-check.sh

echo "=== 智慧大棚平台健康检查 ==="
echo "时间: $(date)"

# 检查前端
echo -n "前端服务: "
if curl -s -o /dev/null -w "%{http_code}" http://localhost:80 | grep -q 200; then
    echo "✅ 正常"
else
    echo "❌ 异常"
fi

# 检查后端
echo -n "后端API: "
if curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/api/v1/health | grep -q 200; then
    echo "✅ 正常"
else
    echo "❌ 异常"
fi

# 检查MQTT
echo -n "MQTT Broker: "
if docker ps | grep -q emqx; then
    echo "✅ 运行中"
else
    echo "❌ 未运行"
fi

# 检查数据库
echo -n "PostgreSQL: "
if docker exec iot_postgres pg_isready -U iot_user -d iot_platform 2>/dev/null | grep -q accepting; then
    echo "✅ 正常"
else
    echo "❌ 异常"
fi

# 检查Redis
echo -n "Redis: "
if docker exec iot_redis redis-cli ping 2>/dev/null | grep -q PONG; then
    echo "✅ 正常"
else
    echo "❌ 异常"
fi

echo "=== 检查完成 ==="
```

#### 4.5.2 日志监控脚本

```bash
#!/bin/bash
# log-monitor.sh

# 监控后端错误日志
echo "监控后端错误日志..."
docker logs -f iot_backend 2>&1 | grep --line-buffered -i "error\|exception\|traceback" | while read line; do
    echo "[$(date)] 发现错误: $line"
    # 可以加告警通知
done
```

### 4.6 持续集成流水线

```yaml
# .github/workflows/test.yml

name: CI/CD Pipeline

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: iot_user
          POSTGRES_PASSWORD: iot_password
          POSTGRES_DB: iot_platform
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
      
      emqx:
        image: emqx/emqx:5.0
        ports:
          - 1883:1883
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      
      - name: Install backend dependencies
        run: |
          cd backend
          pip install -r requirements.txt
      
      - name: Run backend tests
        run: |
          cd backend
          pytest tests/ -v --alluredir=allure-results
      
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      
      - name: Install frontend dependencies
        run: |
          cd frontend
          npm ci
      
      - name: Build frontend
        run: |
          cd frontend
          npm run build
      
      - name: Run UI tests
        run: |
          cd frontend
          npx playwright test
      
      - name: Upload test results
        uses: actions/upload-artifact@v3
        if: always()
        with:
          name: test-results
          path: allure-results
```

---

## 五、测试执行计划

### 5.1 测试阶段划分

| 阶段 | 时间 | 主要工作 | 交付物 |
|------|------|---------|--------|
| 单元测试 | 第1周 | 核心模块单元测试 | 单元测试报告 |
| 集成测试 | 第2周 | 接口联调、模块集成 | 接口测试报告 |
| 系统测试 | 第3周 | 全功能测试、性能测试 | 系统测试报告 |
| 演示测试 | 第4周 | 演示脚本测试、应急演练 | 演示验收报告 |
| 赛前冲刺 | 赛前1周 | 回归测试、模拟比赛 | 最终测试报告 |

### 5.2 缺陷管理

#### 5.2.1 缺陷等级定义

| 等级 | 定义 | 示例 | 修复时限 |
|------|------|------|---------|
| 致命(P0) | 系统崩溃、核心功能不可用 | 登录不上、大屏白屏 | 24小时 |
| 严重(P1) | 主要功能异常、数据错误 | 命令下发失败、数据不对 | 48小时 |
| 一般(P2) | 次要功能问题、UI瑕疵 | 文字错位、提示不准确 | 72小时 |
| 轻微(P3) | 建议性优化、体验问题 | 交互不顺手、样式优化 | 下版本 |

#### 5.2.2 缺陷生命周期

```
新建 → 确认 → 分配 → 修复中 → 待验证 → 已关闭
                ↓                    ↑
              拒绝 ←———————————— 重开
```

### 5.3 质量门禁

| 门禁点 | 通过标准 | 不通过处理 |
|--------|---------|-----------|
| 单元测试 | 覆盖率>70%，通过率100% | 不允许提测 |
| 接口测试 | P0用例100%通过，P1>95% | 打回修复 |
| 系统测试 | P0缺陷0个，P1缺陷≤2个 | 延期发布 |
| 演示测试 | 完整演示3次零失误 | 继续练习 |
| 72小时稳定性 | 无崩溃、无内存泄漏 | 排查问题 |

### 5.4 测试报告模板

```
智慧大棚物联网平台 - 测试报告
================================

一、测试概览
- 测试版本: vX.X
- 测试时间: XXXX-XX-XX ~ XXXX-XX-XX
- 测试人员: XXX
- 测试环境: 环境配置说明

二、测试执行情况
- 用例总数: XXX
- 通过: XXX
- 失败: XXX
- 阻塞: XXX
- 通过率: XX%

三、缺陷统计
- 致命(P0): X个
- 严重(P1): X个
- 一般(P2): X个
- 轻微(P3): X个

四、性能测试结果
- API响应时间: P95=XXXms
- 并发用户数: XXX
- 系统稳定性: XX%

五、风险评估
- 主要风险: ...
- 建议措施: ...

六、测试结论
□ 通过，可以发布
□ 有条件通过（需解决P1问题）
□ 不通过，需重新测试

七、附件
- 详细测试用例
- 缺陷清单
- 性能测试报告
```

---

## 附录

### 附录A：测试环境配置

| 环境 | 用途 | 地址 |
|------|------|------|
| 开发环境 | 开发自测 | localhost |
| 测试环境 | 功能测试 | test.xxx.com |
| 预演环境 | 演示彩排 | demo.xxx.com |
| 比赛环境 | 正式比赛 | 现场部署 |

### 附录B：常用测试工具

| 分类 | 工具 | 用途 |
|------|------|------|
| 接口测试 | Postman / Apifox | 接口调试 |
| 自动化测试 | pytest / Playwright | 自动化 |
| 性能测试 | k6 / JMeter | 性能压测 |
| 抓包工具 | Fiddler / Charles | 网络分析 |
| 浏览器调试 | Chrome DevTools | 前端调试 |
| 数据库工具 | DBeaver / Navicat | 数据验证 |
| MQTT工具 | MQTTX / mosquitto_pub | 设备模拟 |

### 附录C：参考文档

- 需求文档：REQUIREMENTS.md
- 部署文档：DEPLOYMENT.md
- 测试用例：TEST_CASES.md
- API文档：http://localhost:8000/docs

---

**文档版本**: v1.0
**最后更新**: 2026-06-30
**负责人**: 4号 - 测试工程师
